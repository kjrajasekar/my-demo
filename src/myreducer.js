

const myreducer=(state=[], action)=>{
switch(action.type){
   case 'in':
     return state=state.push(data)
   
   case 'de':
      return state-1
      default:
         return state
}
}
export default myreducer